{% extends 'base.html.twig' %}

{% block title %}Utilisateur
{% endblock %}

{% block body %}
	{% if app.user.id == idUser %}
		<h1>Utilisateur</h1>

		<table class="table">
			<tbody>

				<tr>
					<th>Email</th>
					<td>{{ utilisateur.email }}</td>
				</tr>
				<tr>
					<th>Roles</th>
					{% if (utilisateur.roles == ["ROLE_USER"]) %}
						<td>Utilisateur</td>
					{% else %}
						<td>{{ utilisateur.roles ? utilisateur.roles|json_encode : '' }}</td>
					{% endif %}
				</tr>
			</tbody>
		</tr>
		<tr>
			<th>Password</th>
			<td>{{ utilisateur.password }}</td>
		</tr>
	</tbody>
</table>

<a href="{{ path('profil') }}">back to list</a>

<a href="{{ path('utilisateur_edit', {'id': utilisateur.id}) }}">edit</a>

{{ include('utilisateur/_delete_form.html.twig') }}{% else %}
<div class="message">
	Vous ne pouvez pas consulter un autre profil que le votre</div>{% endif %}{% endblock %}
